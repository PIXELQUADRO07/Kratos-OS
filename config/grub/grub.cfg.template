# KratosOS Custom GRUB 2 Configuration Template
# ====================================================
# Generated by KratosOS Build System
# Root UUID:     @ROOT_UUID@
# Root PARTUUID: @ROOT_PARTUUID@
# Build ID:      @BUILD_ID@

set timeout=5
set default=0

# Core modules
insmod part_gpt
insmod ext2
insmod linux

# Serial and console terminal configuration (crucial for headless / QEMU -nographic)
serial --speed=115200 --unit=0 --word=8 --parity=no --stop=1
terminal_input console serial
terminal_output console serial

menuentry "KratosOS 0.1.0 (PARTUUID Auto-Detect)" --class kratos --class os {
    insmod part_gpt
    insmod ext2
    insmod linux
    search --no-floppy --fs-uuid --set=root @ROOT_UUID@
    echo "Loading KratosOS Linux Kernel..."
    linux /boot/vmlinuz root=PARTUUID=@ROOT_PARTUUID@ rw init=/sbin/init console=ttyS0,115200 loglevel=3 kratos.build=@BUILD_ID@ quiet
}

menuentry "KratosOS 0.1.0 (VirtIO /dev/vda2)" --class kratos --class os {
    insmod part_gpt
    insmod ext2
    insmod linux
    search --no-floppy --fs-uuid --set=root @ROOT_UUID@
    echo "Loading KratosOS (VirtIO Target)..."
    linux /boot/vmlinuz root=/dev/vda2 rw init=/sbin/init console=ttyS0,115200 loglevel=3 kratos.build=@BUILD_ID@ quiet
}

menuentry "KratosOS 0.1.0 (Emergency Bash Shell)" --class kratos --class os {
    insmod part_gpt
    insmod ext2
    insmod linux
    search --no-floppy --fs-uuid --set=root @ROOT_UUID@
    echo "Starting emergency maintenance shell..."
    linux /boot/vmlinuz root=PARTUUID=@ROOT_PARTUUID@ rw init=/bin/bash console=ttyS0,115200 loglevel=3 kratos.build=@BUILD_ID@ quiet
}

menuentry "Power Off System" {
    echo "Shutting down KratosOS..."
    halt
}

menuentry "Reboot System" {
    echo "Rebooting..."
    reboot
}
