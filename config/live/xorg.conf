# /etc/X11/xorg.conf — KratosOS Live Environment Configuration
#
# Enables standard server flags and allows auto-detection of GPU drivers
# (modesetting for KMS/DRM/VirtualBox vmwgfx, fbdev for framebuffer, and vesa).

Section "ServerFlags"
    Option "AutoAddDevices" "true"
    Option "AutoEnableDevices" "true"
    Option "DontVTSwitch" "false"
    Option "AllowMouseOpenFail" "true"
EndSection

Section "OutputClass"
    Identifier "modesetting"
    MatchDriver "i915|amdgpu|radeon|nouveau|vmwgfx|bochs-drm|virtio-gpu|simpledrm"
    Driver "modesetting"
    Option "PrimaryGPU" "true"
EndSection
